# 最终PDF逐页审稿与交付记录 v1

> 论文：基于KG-GCN的燃煤机组汽轮机系统的故障诊断方法
>
> 最终构建日期：2026-08-20（UTC+8）
>
> 最终Fast Build：GitHub Actions run `32295100893`
>
> 最终PDF SHA-256：`4676eee674a3d7205cbb942f08bb55b5542fbfc4eff43e127aab59fdb1f6d34b`

## 1. 构建结论

最终XeLaTeX/latexmk构建成功，GitHub Actions中的未定义引用/文献检查通过。

最终PDF结构检查：

- 页数：49页；
- 页面尺寸：A4，595×842 pt；
- 加密：否；
- PyMuPDF可正常打开：是；
- 扫描型PDF：否；
- PDF outline/bookmark：60项；
- undefined citation：0；
- undefined reference：0；
- Overfull hbox：0；
- Underfull hbox：0。

## 2. 全文逐页审稿结论

最终候选PDF已经按“渲染→逐页检查→修改→重新构建→回归比较”的流程完成视觉审稿。

### 前置部分与目录

- 中文摘要、英文Abstract显示完整；
- 摘要与Abstract均进入目录；
- 目录展开至三级标题；
- 前置页使用罗马页码，正文使用阿拉伯页码；
- 目录中的六章、参考文献及页码关系正常。

### 第一章

- 章题“绪论”正确，未发生此前全局section重命名对后续章节的污染；
- 总体技术路线图已由正式矢量图替代历史占位框；
- 正文引用为GB/T 7714数字顺序编码，例如 `[1]`、`[2]` 等；
- 未发现图表越界、正文裁切或孤页。

### 第二章

- 汽轮机系统图、设备级建模框架、GRU结构图、VHP输入输出图、真实训练/验证Loss图、四模型宏平均R²图和代表性预测曲线均进入正式PDF；
- 历史 `\fbox` 图占位已全部消失；
- 24点评价集合说明已明确：24点由后续诊断节点映射规则确定，不依据测试集模型精度筛选；
- 实际GRU结构仍保持 `24×16 → GRU(32) → LayerNorm → Dropout(0.05) → Linear(27)`，未被错误改写为24输出网络；
- 五列表格在杨昊东版心下无越界。

### 第三章

- 知识图谱构建流程图和系统KG总览图均已进入PDF；
- KG总览图采用审计后的保守机组拓扑：VHP一级抽汽→1号高压加热器为已核实直接关系，HP/IP/LP只保留抽汽系统→相应回热设备组的系统级连接；
- 未核实的具体抽汽级次→单台加热器及除氧器蒸汽来源不作为确定性主图关系或GCN投影边；
- 此前“本章小结最后三行独占新页”的孤页已消除；
- 图后审计说明与正文事实口径一致。

### 第四章

- KG-GCN输入构建图、Mask预训练/分类流程图均显示完整；
- 文字方法与冻结v07训练实现一致：两层残差GCN后将 `H^(0)` 与 `H^(2)` 拼接、LayerNorm、按固定DCS节点顺序展平，再进入96维分类层；
- 不再误写成普通全局平均池化GCN；
- 此前章末两行独占一页的问题已通过合理缩放流程图消除。

### 第五章

- 主结果表、3-seed稳定性图、CNN/AE/KG-GCN混淆矩阵、20%标签Mask预训练图位置与结果分析顺序一致；
- 固定seed=123以及3-seed汇总数字与冻结CSV一致；
- CNN和AE的实际结构已写明；
- Train/Validation/Test用途边界已显式写明，测试集不用于标准化、预训练、参数更新、早停或模型选择；
- 故障空间响应 `S_k` 与KG分类邻接结构的独立性已写明，避免循环验证；
- 全文保持“真实健康残差背景 + 独立机理构造的半物理故障场景”口径，未将构造故障写成现场真实故障。

### 第六章

- 总结数字与第二、五章冻结结果一致；
- 结论强度限定在当前代表性半物理场景；
- 对真实现场故障、其他汽轮机设备和仿真泛化均保留进一步验证边界。

### 参考文献

- 已修复最初错误的author-year可选标签显示问题；正文和文末均为数字顺序编码；
- 当前参考文献连续编号 `[1]`–`[38]`；
- natbib采用 `numbers,sort&compress`，文献样式采用 `gbt7714-numerical`；
- 条目字号12 pt、1.25倍行距、无额外条目段后距；
- 最终参考文献占3页；
- 已禁止单条文献跨页拆分，原先reference [28]的arXiv URL页首孤行已经消失；
- 第47–49页已在220 dpi下单独进行高分辨率人工复审。

## 3. 最终视觉回归

最终版只在参考文献分页规则上相较上一版发生变化。使用同一Poppler渲染器、相同100 dpi参数重新渲染两版PDF后比较：

- 第1–46页：46/46页PNG SHA-256完全一致；
- 因此此前已完成的正文逐页视觉审稿结果可直接继承；
- 第47–49页参考文献页重新以220 dpi渲染并人工检查，编号、换行、页间衔接和最后一页均正常。

## 4. 已修复的主要定稿问题

1. GitHub workflow触发等待被替换为可判定的编译日志驱动流程；
2. 显式`.svg`路径导致XeLaTeX失败；
3. 历史图占位文本中的未转义`#`导致编译失败；
4. booktabs `\toprule`全局加粗改写导致 `Misplaced \noalign`；
5. Ch1总体技术路线图占位；
6. Ch2三块历史图占位未真正被wrapper替换；
7. Ch3两张最终KG图未真正进入PDF；
8. Ch3知识图谱总图与v8保守拓扑不一致；
9. Ch3与Ch4章末孤页；
10. 第四章GCN读出描述与实际冻结代码不完全一致；
11. 第五章缺少CNN/AE精确结构和Train/Val/Test边界说明；
12. GB/T 7714参考文献在未加载numeric natbib时错误显示author-year可选标签；
13. 参考文献条目额外间距造成稀疏末页；
14. reference [28] URL跨页产生孤行。

上述问题均已在最终构建中清零。

## 5. 仍需用户本人补充的非研究内容

当前PDF没有编造以下个人/学校元数据：

- 作者姓名；
- 导师姓名及职称；
- 学校；
- 专业/研究方向；
- 学号；
- 答辩或学位授予日期；
- 学校正式学位论文封面与声明页。

这些字段并非论文研究内容缺失，而是用户尚未提供的个人提交信息。当前PDF可作为正文定稿版；正式提交学校前需要按学校模板补齐封面与声明页。

## 6. 最终判定

**Blocking：0**

**Major：0**

当前PDF满足本轮“完整编译成功、引用/交叉引用清零、主图全部落位、逐页视觉审稿完成、关键研究边界一致”的定稿条件，可以作为正文最终交付版本。
