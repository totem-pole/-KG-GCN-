# 第三章 v2 第二轮审稿审计

## Gate summary

### Reviewer 1 — 汽轮机热力系统
- 跨机型证据边界：**PASS**。v2明确地热/核电等文献只作为通用机理知识，需经过本文机组结构与测点兼容性审核，不作为现场故障验证。
- 系统KG与VHP实验衔接：**PASS**。v2明确全系统KG保持系统级完整性，下一章可截取与VHP最终24状态节点相关的参数子图。
- 冷端边界与故障区分：**PASS**。循环水温度/流量继续作为外部边界和影响关系，不被定义为凝汽器本体故障。
- 再热故障知识不足：**OPEN/NON-BLOCKING**。当前只保留结构与接口知识，待获得可靠文献后扩充，不凭经验新增故障边。

### Reviewer 2 — KG/NLP
- 实际构建 vs 自动抽取：**PASS**。v2明确107节点/114三元组由系统拓扑+DCS概念+文献证据人工核验构建；LLM+规则+Embedding用于后续文本扩展。
- 有向多关系图定义：**PASS**。新增$e=(v_i,r,v_j)$，邻接矩阵对称化留到第四章。
- 实体标准化：**PASS**。明确KKS/点表标准名>设备标准名>文献别名，且不虚构$\lambda$/阈值。
- 语义层/测量层：**PASS**。正式定义$\mathcal G_{sem}$与映射$\mathcal M$。
- 图谱规模统计：**PASS**。新增实体规模表与主要关系统计表。

### Reviewer 3 — 结构与写作
- 3.2→3.3过渡：**PASS**。典型故障表收束出五类实体和多关系schema需求。
- 知识来源表：**PASS**。
- 图位：**PASS**。新增构建流程、schema、核心KG、语义层—测量层四张正式占位。
- 第四章边界：**PASS**。第三章仅输出$G_{sem}$和$\mathcal M$，动态偏差、邻接矩阵处理及GCN训练留到第四章。

## Remaining work
1. 依据第三章v2的Figure Contract绘制图3-1~3-4，其中图3-3需要从107节点/114边中筛选可读的代表子图，而不是把全部标签堆入一张图。
2. 从用户全系统总点表继续扩展DCS测量层映射；当前核心语义层不需要等待此工作即可支持文字章节。
3. 如果后续获得具体运行规程/缺陷记录，可运行LLM+规则+语义对齐扩展管线并再报告自动抽取统计。

## Decision
**Blocking concerns: 0**

第三章v2可作为文字先行阶段的导师核稿版，并可继续进入第四章KG-GCN模型文字写作。
